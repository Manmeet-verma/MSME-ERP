{
  "installCommand": "cd backend && npm install",
  "builds": [
    {
      "src": "backend/api/index.ts",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    { "src": "/(.*)", "dest": "backend/api/index.ts" }
  ]
}
