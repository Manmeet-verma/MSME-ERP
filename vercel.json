{
  "installCommand": "npm install -g pnpm@9.15.0 && pnpm install --no-frozen-lockfile",
  "buildCommand": "bash -c 'export PORT=5173 BASE_PATH=/ NODE_ENV=production && cd artifacts/quotation-app && pnpm exec vite build --config vite.config.ts'",
  "outputDirectory": "artifacts/quotation-app/dist",
  "framework": null
}
