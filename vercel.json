{
  "buildCommand": "pnpm --filter @workspace/api-server run build && pnpm --filter @workspace/quotation-app run build",
  "installCommand": "pnpm install --no-frozen-lockfile",
  "framework": null
}
