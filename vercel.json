{
  "installCommand": "cd backend && npm install && npm run build",
  "builds": [
    {
      "src": "backend/api/index.js",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    { "src": "/(.*)", "dest": "backend/api/index.js" }
  ]
}
