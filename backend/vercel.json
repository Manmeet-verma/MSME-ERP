{
  "installCommand": "npm install && npm run build",
  "builds": [
    {
      "src": "api/index.cjs",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    { "src": "/api/(.*)", "dest": "api/index.cjs" },
    { "src": "/(.*)", "dest": "api/index.cjs" }
  ]
}
