{
  "installCommand": "npm install && npm run build",
  "buildCommand": "",
  "builds": [
    {
      "src": "api/index.cjs",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    { "src": "/(.*)", "dest": "api/index.cjs" }
  ]
}
